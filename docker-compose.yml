version: '2'

services:
#  compilador:
#    build:
#      context: .
#      dockerfile: Dockerfile_java
#    volumes_from:
#      - ./:/educodar

  proxy:
   build:
      context: .
      dockerfile: Dockerfile_proxy
   ports:
    - 80:80
